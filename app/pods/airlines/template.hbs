<main class="airlines-page content-card full-page-max-width">
  <h1 class="mt-3 mb-3">
    Airlines
    <small class="text-muted d-none d-sm-inline">Resource Page</small>
  </h1>

  <p class="text-right">
    <button type="button" class="btn btn-success" {{action "createButtonWasPressed"}}>
      <i class="fas fa-plus"></i>
      New
    </button>
  </p>

  {{#each records as |record|}}
    {{airline/record-manager record}}
  {{/each}}
</main>