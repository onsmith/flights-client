<table class="record-table">
  <tbody>
    {{#unless record.isNew}}
      <tr class="record-field">
        <td class="record-key">id:</td>
        <td class="record-value">{{record.id}}</td>
      </tr>
    {{/unless}}
    <tr class="record-field">
      <td class="record-key">departs_at:</td>
      <td class="record-value">"{{record.departs_at}}"</td>
    </tr>
    <tr class="record-field">
      <td class="record-key">arrives_at:</td>
      <td class="record-value">"{{record.arrives_at}}"</td>
    </tr>
    <tr class="record-field">
      <td class="record-key">number:</td>
      <td class="record-value">"{{record.number}}"</td>
    </tr>
    <tr class="record-field">
      <td class="record-key">plane_id:</td>
      <td class="record-value">{{record.plane_id}}</td>
    </tr>
    <tr class="record-field">
      <td class="record-key">departure_id:</td>
      <td class="record-value">{{record.departure_id}}</td>
    </tr>
    <tr class="record-field">
      <td class="record-key">arrival_id:</td>
      <td class="record-value">{{record.arrival_id}}</td>
    </tr>
    <tr class="record-field">
      <td class="record-key">next_flight_id:</td>
      <td class="record-value">{{record.next_flight_id}}</td>
    </tr>
    <tr class="record-field">
      <td class="record-key">airline_id:</td>
      <td class="record-value">{{record.airline_id}}</td>
    </tr>
    <tr class="record-field">
      <td class="record-key">info:</td>
      <td class="record-value">
        {{textarea class="form-control" value=record.info}}
      </td>
    </tr>
    {{#unless record.isNew}}
      <tr class="record-field">
        <td class="record-key">user_id:</td>
        <td class="record-value">{{record.user_id}}</td>
      </tr>
      <tr class="record-field">
        <td class="record-key">created_at:</td>
        <td class="record-value">"{{record.created_at}}"</td>
      </tr>
      <tr class="record-field">
        <td class="record-key">updated_at:</td>
        <td class="record-value">"{{record.updated_at}}"</td>
      </tr>
    {{/unless}}
  </tbody>
</table>

<p class="record-controls mt-3">
  <button type="button" class="btn btn-info" {{action "saveButtonWasPressed"}}>
    <i class="far fa-save"></i>&nbsp; Save
  </button>

  <button type="button" class="btn btn-secondary" {{action "cancelButtonWasPressed"}}>
    <i class="fas fa-times"></i>&nbsp; Cancel
  </button>
</p>